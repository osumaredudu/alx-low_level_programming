#include "main.h"

/**
 * _islower - prints 1 if c is lowercase and 0 for otherwise
 * @c: first parameter
 * Description: program check for lowercase
 * Return: 0 if no error occured
 */

int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
